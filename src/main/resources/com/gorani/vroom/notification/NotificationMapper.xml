<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gorani.vroom.notification.NotificationMapper">

    <insert id="insertNotification" parameterType="com.gorani.vroom.notification.NotificationVO">
        INSERT INTO NOTIFICATIONS (user_id, type, message, url)
        VALUES (#{userId}, #{type}, #{message}, #{url})
    </insert>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM NOTIFICATIONS
        WHERE user_id = #{userId}
          AND is_read = FALSE
    </select>

    <select id="selectMyNotifications" resultType="com.gorani.vroom.notification.NotificationVO">
        SELECT *
        FROM NOTIFICATIONS
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT 20
    </select>

    <update id="updateReadStatus">
        UPDATE NOTIFICATIONS
        SET is_read = TRUE
        WHERE notification_id = #{notificationId}
    </update>

    <update id="updateAllRead">
        UPDATE NOTIFICATIONS
        SET is_read = TRUE
        WHERE user_id = #{userId}
    </update>


</mapper>